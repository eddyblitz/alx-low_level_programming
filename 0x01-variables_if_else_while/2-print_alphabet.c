#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase
 *
 * Always 0 (Success)
 *
 */

int main(void)

{

char be;

for (be = 'a'; be <= 'z'; be++)
putchar(be);
putchar('\n');

return (0);
}
